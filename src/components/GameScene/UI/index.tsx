import styled from "styled-components";
import "./index.scss";
import { StartMenu } from "./StartMenu";
export const UI = () => {
    return (
        <div className="content">
            <StartMenu />
        </div>
    );
};
